<?php


//checking the user-role [PDC]
function issetSession($userId){
            // if (!isset($_SESSION['user_id'])) { //If the user not logged, redirected to login(PDC) 
        //     redirect('users/pdc-login');
        // }
}